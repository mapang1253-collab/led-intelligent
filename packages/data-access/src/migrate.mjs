#!/usr/bin/env node
// Runs SQL migrations in database/migrations/ against the configured Supabase Postgres instance.
// Placeholder — no migrations exist yet (Day 2-3 of docs/adr/0001-first-increment-scope.md).
console.log("db:migrate — no migrations in database/migrations/ yet. Nothing to do.");
