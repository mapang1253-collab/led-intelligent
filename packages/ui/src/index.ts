/**
 * @reis/ui — shared React/shadcn-style components (docs/technology-stack.md §5: PropertyIntakeForm,
 * ProvinceSelect, AnalysisProgress, ResultHeader, EvidenceCard, ...). Consumes only public contracts;
 * never imports server modules (data-access, source-adapters, ai-gateway).
 *
 * WP9 scaffold (Day 7-10 of the current increment). Empty until the intake screen work starts.
 */
export {};
