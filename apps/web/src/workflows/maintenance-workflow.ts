/**
 * MaintenanceWorkflow — scheduled/bulk source ingestion via Cron Triggers (docs/technology-stack.md
 * §3). Out of scope for the current increment (docs/adr/0001-first-increment-scope.md): the first
 * source adapters fetch on demand during a run rather than through a scheduled bulk job.
 */
export {};
