# Copy to .env.migration (git-ignored) and fill in real values. Used only by `pnpm db:migrate` /
# `pnpm db:seed:academic` (local Node.js tooling — never shipped to the Worker).

# Supabase direct Postgres connection string (same source as apps/web/.dev.vars DATABASE_URL).
MIGRATION_DATABASE_URL=postgresql://
